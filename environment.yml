name: cbi_toolbox
channels:
  - conda-forge
  - defaults
dependencies:
  - cmake>=3.18*
  - cxx-compiler
  - python>=3*
  - numpy>=1.17*
  - scipy>=1.4*
  - noise>=1.2*
  - scikit-image
  - mpi4py
  - pip
  - pip:
      - opencv-python
      - apeer-ometiff-library
      - poppy
